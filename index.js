/**
 * @format
 */

import {AppRegistry} from 'react-native';

import {name as appName} from './app.json';
import TheMapsApp from './src/TheMapsApp';

AppRegistry.registerComponent(appName, () => TheMapsApp);
